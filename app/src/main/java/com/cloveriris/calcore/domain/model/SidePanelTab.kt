package com.cloveriris.calcore.domain.model

/**
 * 右侧面板 / 副面板 Tab 类型
 */
enum class SidePanelTab {
    NONE,           // 显示按键
    HISTORY,        // 历史记录
    MEMORY,         // 内存
    VISUALIZATION   // 可视化（手机竖屏时显示可视化内容）
}
