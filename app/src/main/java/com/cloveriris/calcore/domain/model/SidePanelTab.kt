package com.cloveriris.calcore.domain.model

/**
 * 右侧面板 / 副面板 Tab 类型
 */
enum class SidePanelTab {
    NONE,       // 隐藏副面板，显示按键
    HISTORY,    // 历史记录
    MEMORY,     // 内存
    VISUALIZATION // 可视化（横屏专用）
}
